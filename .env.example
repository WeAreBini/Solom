# Financial Modeling Prep API Key
# Get your free API key at: https://site.financialmodelingprep.com/developer/docs
FMP_API_KEY=your_api_key_here

# WebSocket URL for real-time price streaming (optional)
# If not set, the app will use polling fallback (5-second intervals)
# Example: wss://your-websocket-server.com/stream or ws://localhost:8080/stream
# For Polygon.io (requires separate subscription): wss://socket.polygon.io/stocks
NEXT_PUBLIC_WS_URL=

# WebSocket URL for real-time price streaming (optional)
# If not set, the app will use polling for price updates
# Example: wss://api.example.com/ws or ws://localhost:8080/ws
NEXT_PUBLIC_WS_URL=